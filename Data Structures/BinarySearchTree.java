public class BinarySearchTree<T> {
    TreeNode root;
    public BinarySearchTree() {
        root = new TreeNode();
    }
    public BinarySearchTree(T data) {
        root = new TreeNode(data);
    }
    public boolean add(T data) {
        // if (root == null) {
        //     root = new TreeNode(data);
        //     return true;
        // }
        // TreeNode tempRoot = root;
        // while (tempRoot != null) {
        //     if (tempRoot.get() > data) {
        //
        //     }
        // }
        return true;
    }

}
